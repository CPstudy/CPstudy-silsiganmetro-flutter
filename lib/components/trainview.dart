import 'package:flutter/material.dart';
import 'package:silsiganmetro/dto/traindto.dart';

class TrainView extends StatelessWidget {

  int type = 0;
  List<TrainDTO> list = new List();

  TrainView(this.type, this.list);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
